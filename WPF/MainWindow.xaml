<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" ScrollViewer.CanContentScroll="True">

    <Grid Name="MainGrid" Margin="8,0,0.2,0">
        <Grid.Background>
            <ImageBrush ImageSource="images\fitness-stuff-grey-background.jpg" Stretch="Fill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <UserControl Name="ButtonsUserControl" Grid.Row="0" Content="{Binding MainWindowButtons}" Opacity="0.2" BorderThickness="0" Margin="5,0,708.2,0" />
        <UserControl Padding="0,10,0,0"  Name="userControl" Content="{Binding UserControl}"  BorderThickness="0" Margin="9,56,10.2,0" ScrollViewer.CanContentScroll="True" SnapsToDevicePixels="True" Grid.RowSpan="2" />

    </Grid>
</Window>
